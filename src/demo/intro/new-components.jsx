import React from 'react'

const newComponents = ({ provider: WPProvider, ...rest }) => {
  return (
    <section {...rest}>
      <h3>Composing New Components</h3>

    </section>
  )
}

export default newComponents
